{
  "name": "backend",
  "version": "1.0.0",
  "description": "Lambda 函数后端",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch functions/local-server.js",
    "test": "jest",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.js\""
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.490.0",
    "@aws-sdk/lib-dynamodb": "^3.490.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^3.0.1",
    "fast-check": "^3.15.1",
    "jest": "^29.7.0"
  }
}
